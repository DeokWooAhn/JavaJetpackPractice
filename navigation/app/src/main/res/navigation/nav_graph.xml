<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/home">

    <fragment
        android:id="@+id/home"
        android:name="com.example.navigation.HomeFragment"
        android:label="@string/home"
        tools:layout="@layout/fragment_home" />
    <fragment
        android:id="@+id/search"
        android:name="com.example.navigation.SearchFragment"
        android:label="@string/search"
        tools:layout="@layout/fragment_search" />
    <fragment
        android:id="@+id/like"
        android:name="com.example.navigation.LikeFragment"
        android:label="@string/like"
        tools:layout="@layout/fragment_like" />
    <fragment
        android:id="@+id/user"
        android:name="com.example.navigation.UserFragment"
        android:label="@string/user"
        tools:layout="@layout/fragment_user" />
</navigation>